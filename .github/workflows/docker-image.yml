name: Docker Image CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Login to Docker Registry
      run: echo "${{ secrets.DOCKER_ACCESS_TOKEN }}" | docker login -u ${{ secrets.DOCKER_USERNAME }} --password-stdin docker.io

    - name: Build the base Docker image
      run: docker build base-image/ --file base-image/Dockerfile --tag docker.io/my-base-image-name:latest

    - name: Push the base Docker image
      run: docker push docker.io/my-base-image-name:latest

    - name: Build and push service Docker images
      run: |
        for service in api-gateway frontend job-handler knowledge-base mzn-pod sat-pod maxsat-pod; do
          docker build services/$service --file services/$service/Dockerfile --tag docker.io/$service:latest
          docker push docker.io/$service:latest
        done
