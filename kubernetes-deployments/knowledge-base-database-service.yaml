apiVersion: v1
kind: Service
metadata:
  name: knowledge-base-database-service
spec:
  selector:
    app: knowledge-base-database
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
  type: ClusterIP
